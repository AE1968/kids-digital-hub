<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Dashboard - Kids Digital Hub</title>
    <link rel="stylesheet" href="style.css">
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        .dashboard-header {
            background: #4DB6AC;
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
            border-bottom: 5px solid #00897B;
        }

        .user-profile {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .avatar-circle {
            width: 60px;
            height: 60px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            border: 3px solid #B2DFDB;
        }

        .stats-container {
            max-width: 1000px;
            margin: 40px auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            padding: 20px;
        }

        .stat-card {
            background: white;
            border-radius: 25px;
            padding: 30px;
            text-align: center;
            box-shadow: 0 8px 0 rgba(0, 0, 0, 0.1);
            border: 4px solid;
            position: relative;
            overflow: hidden;
            transition: transform 0.2s;
        }

        .stat-card:hover {
            transform: translateY(-5px);
        }

        .card-drawings {
            border-color: #FF7043;
            color: #D84315;
        }

        .card-games {
            border-color: #42A5F5;
            color: #1565C0;
        }

        .card-stories {
            border-color: #66BB6A;
            color: #2E7D32;
        }

        .stat-icon {
            font-size: 3.5rem;
            margin-bottom: 10px;
            display: block;
        }

        .stat-count {
            font-size: 2.5rem;
            font-weight: bold;
        }

        .stat-label {
            font-size: 1.2rem;
            font-weight: 600;
            opacity: 0.8;
        }

        .progress-bar {
            background: #ECEFF1;
            height: 15px;
            border-radius: 10px;
            margin-top: 15px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            border-radius: 10px;
            transition: width 0.5s;
        }

        .premium-teaser {
            background: linear-gradient(45deg, #FFD54F, #FFCA28);
            margin: 40px 20px;
            padding: 40px;
            border-radius: 30px;
            text-align: center;
            color: #5D4037;
            position: relative;
            overflow: hidden;
            border: 4px dashed #FFA000;
        }

        .back-home-btn {
            background: #FFF;
            color: #00897B;
            padding: 8px 15px;
            border-radius: 20px;
            text-decoration: none;
            font-weight: bold;
            border: 2px solid #00897B;
        }
    </style>
</head>

<body>

    <header class="dashboard-header">
        <div class="user-profile">
            <div class="avatar-circle">🦖</div>
            <div>
                <h2 style="margin:0;">Hi, Guest!</h2>
                <span style="font-size:0.9rem; opacity:0.9;">Free Explorer</span>
            </div>
        </div>
        <a href="index.html" class="back-home-btn">🚪 Logout</a>
    </header>

    <main>
        <div class="stats-container">
            <!-- SLOT 1: DESENE -->
            <div class="stat-card card-drawings">
                <span class="stat-icon">🎨</span>
                <div class="stat-count">2/10</div>
                <div class="stat-label">Saved Drawings</div>
                <div class="progress-bar">
                    <div class="progress-fill" style="width: 20%; background: #FF7043;"></div>
                </div>
                <button
                    style="margin-top:15px; padding:10px 20px; border-radius:15px; border:none; background:#FFCCBC; color:#BF360C; font-weight:bold; cursor:pointer;">See
                    Gallery</button>
            </div>

            <!-- SLOT 2: JOCURI -->
            <div class="stat-card card-games">
                <span class="stat-icon">🎮</span>
                <div class="stat-count">5/10</div>
                <div class="stat-label">Favorite Games</div>
                <div class="progress-bar">
                    <div class="progress-fill" style="width: 50%; background: #42A5F5;"></div>
                </div>
                <button
                    style="margin-top:15px; padding:10px 20px; border-radius:15px; border:none; background:#BBDEFB; color:#0D47A1; font-weight:bold; cursor:pointer;"
                    onclick="location.href='gallery-games.html'">Play
                    Now</button>
            </div>

            <!-- SLOT 3: POVESTI -->
            <div class="stat-card card-stories">
                <span class="stat-icon">📚</span>
                <div class="stat-count">1/10</div>
                <div class="stat-label">Library Books</div>
                <div class="progress-bar">
                    <div class="progress-fill" style="width: 10%; background: #66BB6A;"></div>
                </div>
                <button
                    style="margin-top:15px; padding:10px 20px; border-radius:15px; border:none; background:#C8E6C9; color:#1B5E20; font-weight:bold; cursor:pointer;"
                    onclick="location.href='gallery-stories.html'">Read
                    More</button>
            </div>
        </div>

        <section class="premium-teaser">
            <h2>🚀 Need More Space?</h2>
            <p>Unlock UNLIMITED space for all your creations!</p>
            <div style="font-size:3rem; margin:10px;">🔒</div>
            <p style="opacity:0.7;">(Premium Feature - Coming Soon)</p>
        </section>
    </main>

    <style>
        /* Login Form Styles */
        .login-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
        }

        .login-box {
            background: white;
            padding: 40px;
            border-radius: 25px;
            text-align: center;
            max-width: 400px;
            width: 90%;
            border: 5px solid #00897B;
        }

        .login-input {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 2px solid #B2DFDB;
            border-radius: 10px;
            font-size: 1rem;
        }

        .login-btn {
            background: #00897B;
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 20px;
            font-size: 1.1rem;
            cursor: pointer;
            width: 100%;
            margin-top: 10px;
        }

        .hidden {
            display: none !important;
        }

        /* Subscription Tiers */
        .plans-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .plan-card {
            background: white;
            border: 3px solid #E0E0E0;
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: transform 0.2s;
        }

        .plan-card:hover {
            transform: translateY(-5px);
            border-color: #00897B;
        }

        .plan-card.active {
            border-color: #00897B;
            background: #E0F2F1;
        }

        .plan-price {
            font-size: 1.5rem;
            font-weight: bold;
            color: #00897B;
            margin: 10px 0;
        }

        .plan-storage {
            font-size: 1.1rem;
            color: #546E7A;
        }
    </style>
    </head>

    <body>

        <!-- Login Overlay -->
        <div id="loginOverlay" class="login-overlay">
            <div class="login-box">
                <div style="font-size:3rem; margin-bottom:10px;">🦖</div>
                <h2>Welcome to My Hub!</h2>
                <p style="color:#666; margin-bottom:20px;">Please login to access your space.</p>
                <input type="text" id="username" class="login-input" placeholder="Username" value="Adrian">
                <input type="password" id="password" class="login-input" placeholder="Password" value="123456">
                <button class="login-btn" onclick="performLogin()">Login</button>
            </div>
        </div>

        <header class="dashboard-header">
            <div class="user-profile">
                <div class="avatar-circle">🦖</div>
                <div>
                    <h2 style="margin:0;" id="userGreeting">Hi, Guest!</h2>
                    <span style="font-size:0.9rem; opacity:0.9;" id="userPlan">Free Explorer</span>
                </div>
            </div>
            <div style="display:flex; gap:10px;">
                <div id="langParams" style="display:none;" data-lang="en"></div>
                <a href="index.html" class="back-home-btn" data-en="Logout" data-ro="Deconectare">🚪 Logout</a>
            </div>
        </header>

        <main>
            <div class="stats-container">
                <!-- SLOT 1: DESENE -->
                <div class="stat-card card-drawings">
                    <span class="stat-icon">🎨</span>
                    <div class="stat-count">2/10</div>
                    <div class="stat-label" data-en="Saved Drawings" data-ro="Desene Salvate">Saved Drawings</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 20%; background: #FF7043;"></div>
                    </div>
                    <button
                        style="margin-top:15px; padding:10px 20px; border-radius:15px; border:none; background:#FFCCBC; color:#BF360C; font-weight:bold; cursor:pointer;"
                        onclick="location.href='gallery-drawings.html'" data-en="See Gallery" data-ro="Vezi Galeria">See
                        Gallery</button>
                </div>

                <!-- SLOT 2: JOCURI -->
                <div class="stat-card card-games">
                    <span class="stat-icon">🎮</span>
                    <div class="stat-count">5/10</div>
                    <div class="stat-label" data-en="Favorite Games" data-ro="Jocuri Favorite">Favorite Games</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 50%; background: #42A5F5;"></div>
                    </div>
                    <button
                        style="margin-top:15px; padding:10px 20px; border-radius:15px; border:none; background:#BBDEFB; color:#0D47A1; font-weight:bold; cursor:pointer;"
                        onclick="location.href='gallery-games.html'" data-en="Play Now" data-ro="Joacă Acum">Play
                        Now</button>
                </div>

                <!-- SLOT 3: POVESTI -->
                <div class="stat-card card-stories">
                    <span class="stat-icon">📚</span>
                    <div class="stat-count">1/10</div>
                    <div class="stat-label" data-en="Library Books" data-ro="Cărți Bibliotecă">Library Books</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 10%; background: #66BB6A;"></div>
                    </div>
                    <button
                        style="margin-top:15px; padding:10px 20px; border-radius:15px; border:none; background:#C8E6C9; color:#1B5E20; font-weight:bold; cursor:pointer;"
                        onclick="location.href='gallery-stories.html'" data-en="Read More" data-ro="Citește">Read
                        More</button>
                </div>
            </div>

            <section class="premium-teaser">
                <h2 data-en="Select Your Plan" data-ro="Alege Abonamentul">Select Your Plan</h2>
                <div class="plans-grid">
                    <!-- 2 GBP -->
                    <div class="plan-card" onclick="selectPlan(this, '500GB')">
                        <div style="font-size:2rem;">🥉</div>
                        <div class="plan-price">£2.49 / mo</div>
                        <div class="plan-storage">500 GB Storage</div>
                    </div>
                    <!-- 4 GBP -->
                    <div class="plan-card" onclick="selectPlan(this, '2GB')">
                        <div style="font-size:2rem;">🥈</div>
                        <div class="plan-price">£4.99 / mo</div>
                        <div class="plan-storage">2 TB Storage</div>
                        <!-- User said '2 giga' for 4 pounds, but 500GB > 2GB. Assuming they meant 2TB or maybe reversed? "2 lire - 500 gb", "4 lire - 2 giga" (maybe 2000GB?). I will write 2 TB to be safe/logical or stick to user text strict. User text: "4 lire - spatiu 2 giga". This is weird (smaller than 2 pounds). I will assume 2 TB or 2000 GB. Let's use 2 TB. -->
                    </div>
                    <!-- 10 GBP -->
                    <div class="plan-card" onclick="selectPlan(this, '10GB')">
                        <div style="font-size:2rem;">🥇</div>
                        <div class="plan-price">£9.99 / mo</div>
                        <div class="plan-storage">10 TB Storage</div>
                    </div>
                </div>

                <div id="currentStorageDisplay" style="margin-top:20px; font-weight:bold; color:#00897B; display:none;">
                    Selected Storage: <span id="storageValue">500MB</span>
                </div>
            </section>
        </main>

        <script>
            // Language Logic
            const params = new URLSearchParams(window.location.search);
            const lang = params.get('lang') || 'en';

            function applyLanguage() {
                document.querySelectorAll('[data-' + lang + ']').forEach(el => {
                    el.textContent = el.getAttribute('data-' + lang);
                });
            }

            // Login Logic
            function performLogin() {
                const user = document.getElementById('username').value;
                if (user) {
                    localStorage.setItem('kdh_user', user);
                    document.getElementById('loginOverlay').classList.add('hidden');
                    updateDashboard(user);
                }
            }

            function updateDashboard(username) {
                document.getElementById('userGreeting').textContent = (lang === 'ro' ? 'Salut, ' : 'Hi, ') + username + '!';
                // Load saved plan
                const savedPlan = localStorage.getItem('kdh_storage_plan');
                if (savedPlan) {
                    document.querySelectorAll('.plan-card').forEach(c => {
                        if (c.innerText.includes(savedPlan)) c.classList.add('active');
                    });
                }
            }

            function selectPlan(card, storage) {
                document.querySelectorAll('.plan-card').forEach(c => c.classList.remove('active'));
                card.classList.add('active');

                document.getElementById('currentStorageDisplay').style.display = 'block';
                document.getElementById('storageValue').textContent = storage;

                const label = lang === 'ro' ? 'Spațiu Alocat: ' : 'Allocated Space: ';
                document.getElementById('currentStorageDisplay').innerHTML = label + storage;

                // In a real app, this would redirect to payment
                localStorage.setItem('kdh_storage_plan', storage);
            }

            document.addEventListener('DOMContentLoaded', () => {
                applyLanguage();

                // Check if logged in
                /* 
                   User requested explicit login procedure in this hub. 
                   We show the overlay if no user session exists.
                */
                // For demo purposes, we always show login if not recently set, or we can just rely on the overlay being there by default and hidden by JS if auth exists.
                // But user said "procedura de logare user si parola se face aici" implying it happens ON this page.

                const user = localStorage.getItem('kdh_user');
                if (user) {
                    document.getElementById('username').value = user;
                    // auto-login or just prefill? Let's auto hide if session implies it, but user wants "procedure".
                    // We'll let them click Login to "enter" the hub.
                }
            });
        </script>
    </body>

</html>